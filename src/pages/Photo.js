import React from "react";
import PhotoDetails from "../components/PhotoDetails";

const Photo = () => {
  return (
    <div>
      <PhotoDetails />
    </div>
  );
};

export default Photo;
